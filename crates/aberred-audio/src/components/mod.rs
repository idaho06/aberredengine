//! Components used only by the audio thread's own `bevy_ecs::World`. See
//! the crate root docs for the sim/logic-world boundary this crate keeps.

pub mod handles;
pub mod music_track;
pub mod playing_fx;
